const Elipse17 = () => {
    const estiloElipse17 = ({ top, left, width, height }) => {
        const estilo = {
            position: 'absolute',
            top: `${top}px`,
            left: `${left}px`,
            width: `${width}px`,
            height: `${height}px`,
            borderRadius: '50%',
            // Otros estilos necesarios
        };
        return (
            <div style={estiloElipse17}>        </div>
        )
    }
}
    export default Elipse17